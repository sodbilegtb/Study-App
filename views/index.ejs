<body>
    <div class="text-center">
        <% if (locals.user) { %>
            <h2>Welcome back <%= locals.user.name.first %>!</h2>
        <% } else { %>
            <h2>Welcome!</h2>
            <a href="/users/login">Login</a><br>
            <a href="/users/new">Create an account</a>
        <% } %>
    </div>
</body>
